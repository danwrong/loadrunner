

var mod = module('name', function(exports) {
  // your code
  
  exports({
    // export anything you want - typically an object containing methods
  });
});

mod.then(function() {
  // exports are complete
});